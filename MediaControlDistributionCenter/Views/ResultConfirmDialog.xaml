<UserControl x:Class="MediaControlDistributionCenter.Views.ResultConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MediaControlDistributionCenter.Views"
             xmlns:converters="clr-namespace:MediaControlDistributionCenter.Converters"
             xmlns:viewmodels="clr-namespace:MediaControlDistributionCenter.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="197" d:DesignWidth="384">
    <UserControl.Resources>
        <local:ConfirmDialogDataTemplateSelector x:Key="ConfirmDialogDataTemplateSelector"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <DataTemplate x:Key="UserRegisterSuccess" DataType="{x:Type viewmodels:UserViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Width="80" Foreground="White" Text="{DynamicResource LanguageKey_Code_LoginAccount}" />
                    <TextBlock Width="250" Foreground="White" Text="{Binding Name}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Width="80" Foreground="White" Text="{DynamicResource LanguageKey_Code_LoginPassword}" />
                    <TextBlock Width="250" Foreground="White" Text="{Binding Password}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="MediaContentSave" DataType="{x:Type viewmodels:MediaViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" Text ="节目：" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text="{Binding Name}" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" Text =" 内容已更新！" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="MediaContentPublish" DataType="{x:Type viewmodels:MediaDevicesViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" Text ="节目：" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text="{Binding CurrentMedia.Name}" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" Text =" 发布结果：" />
                </StackPanel>
                <Border Grid.Row="2" Margin="10" Width="500" BorderThickness="0" Background="#16171D">
                    <Grid>
                        <Border Background="#1A1E30" Height="40" VerticalAlignment="Top" />
                        <DataGrid 
                            AutoGenerateColumns="False" 
                            CanUserAddRows="False"  
                            FontSize="14" 
                            IsReadOnly="True" 
                            SelectionMode="Single"
                            FrozenColumnCount="1" 
                            Background="Transparent" 
                            ItemsSource="{Binding PublishDevices}"
                        >
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="1*" Binding="{Binding Name}" Header="名称" />
                                <DataGridTextColumn Width="1*" Binding="{Binding SNumber}" Header="SN码" />
                                <DataGridTextColumn Width="1*" Binding="{Binding Resolution}" Header="分辨率"  />
                                <DataGridTextColumn Width="1*" Binding="{Binding UploadResult}" Header="上传状态"  />
                                <DataGridTextColumn Width="1*" Binding="{Binding SendResult}" Header="发送状态"  />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ScheduleControlExecution" DataType="{x:Type viewmodels:DeviceTimeControlViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text ="创建 " />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text="{Binding Type}" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text =" 定时控制成功！" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize = "14" Text ="是否立即执行？" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" >
                    <Button Foreground="WhiteSmoke" Content="是" Click="btnExecute" />
                    <Button Foreground="WhiteSmoke" Margin="50,0,0,0" Content="否" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ScheduleSendUserExecution" DataType="{x:Type viewmodels:DeviceViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text ="连接设备 " />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text="{Binding Name}" />
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize ="16" Text =" 成功！" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize = "14" Text ="是否录入当前用户？" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" >
                    <Button Foreground="WhiteSmoke" Content="是" Click="btnExecuteSendUser" />
                    <Button Foreground="WhiteSmoke" Margin="50,0,0,0" Content="否" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="SyncUserResult" DataType="{x:Type viewmodels:LoginViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" Text ="{DynamicResource LanguageKey_Code_Device_Tooltip_102}" />
                </StackPanel>
                <Border Grid.Row="2" Margin="10" Width="300" BorderThickness="0" Background="#16171D">
                    <Grid>
                        <Border Background="#1A1E30" Height="40" VerticalAlignment="Top" />
                        <DataGrid 
                            AutoGenerateColumns="False" 
                            CanUserAddRows="False"  
                            FontSize="14" 
                            IsReadOnly="True" 
                            SelectionMode="Single"
                            FrozenColumnCount="1" 
                            Background="Transparent" 
                            ItemsSource="{Binding SyncUsers}"
                        >
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="1*" Binding="{Binding}" Header="{DynamicResource LanguageKey_Code_LoginAccount}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="LoginUserResult" DataType="{x:Type viewmodels:LoginViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize="14" Text ="{Binding VerifyResult}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ManageUserResult" DataType="{x:Type viewmodels:UserManageViewModel}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10" >
                    <TextBlock Foreground="White" VerticalAlignment="Center" FontSize="14" Text ="{Binding ErrorMessage}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel LastChildFill="False" Background="#2A3255">
        <Grid HorizontalAlignment="Center" Margin="10" DockPanel.Dock="Top"  Width="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="2" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="10">
                <TextBlock VerticalAlignment="Center" DockPanel.Dock="Left" FontSize="20" Foreground="White" Text="{DynamicResource LanguageKey_Code_Confirm_DialogTitle}" />
            </StackPanel>

            <Border Grid.Row="1" Width="auto" Grid.ColumnSpan="2" Background="#40476F"/>
            <StackPanel Grid.Row="2" Margin="10">
                <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource ConfirmDialogDataTemplateSelector}" />
            </StackPanel>
        </Grid>
        <Grid DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,25">
                <Button x:Name="btnConfirm" DockPanel.Dock="Left" Foreground="WhiteSmoke" Content="{DynamicResource LanguageKey_Code_Confirm}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" />
            </StackPanel>
        </Grid>
    </DockPanel>
    
</UserControl>
