<UserControl x:Class="MediaControlDistributionCenter.Views.Diagrams.InputDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaControlDistributionCenter.Views.Diagrams"
             xmlns:converters="clr-namespace:MediaControlDistributionCenter.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             >
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:IsEditingValueConverter x:Key="IsEditingValueConverter" />
        <converters:SplitStringConverter x:Key="SplitStringConverter" />
        <converters:SelectedValueConverter x:Key="SelectedValueConverter" />
        <converters:PercentageConverter x:Key="PercentageConverter" />
        <converters:MultiSelectedValueConverter x:Key="MultiSelectedValueConverter" />
    </UserControl.Resources>
    <Grid>
        <DockPanel LastChildFill="False" Background="#090A0E">
            <Grid Width="400" Height="200" HorizontalAlignment="Center" Margin="10" DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="2" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="10" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" FontSize="20" Foreground="White" Text="输入值" />
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Margin="10">
                    <Button Width="60" Height="38" HorizontalAlignment="Right"
                    Background="#090A0E" Foreground="WhiteSmoke"
                    BorderThickness="0"
                    HorizontalContentAlignment="Center"
                    materialDesign:ColorZoneAssist.Mode="Custom"
                    materialDesign:ColorZoneAssist.Background="#090A0E"
                    materialDesign:ButtonAssist.CornerRadius="5"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Content="{materialDesign:PackIcon Size=20,Kind=CloseCircleOutline}"/>
                </StackPanel>

                <Border Grid.Row="1" Width="auto" Background="#40476F"/>

                <StackPanel Grid.Row="2" Margin="10, 20, 10, 10">
                    <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
                        <TextBlock VerticalAlignment="Center" FontSize="16" Width="60" Text="值" />
                        <TextBox VerticalAlignment="Center" Width="250" Height="32" Background="#2A3255" Padding="5" BorderBrush="#404870" BorderThickness="1" Name="tbResult" FontSize="16" />
                    </StackPanel>
                </StackPanel>
            </Grid>
            <Grid DockPanel.Dock="Bottom">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,25" Width="209">
                    <Button DockPanel.Dock="Left" Foreground="WhiteSmoke" Content="{DynamicResource LanguageKey_Code_Confirm}" Click="btnConfirm_Click" />
                    <Button DockPanel.Dock="Right" Margin="25" Background="#090A0E" Foreground="WhiteSmoke" Content="{DynamicResource LanguageKey_Code_Cancel}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand }" />
                </StackPanel>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
