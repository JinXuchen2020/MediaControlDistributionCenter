<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:MediaControlDistributionCenter.Views.CustomControls">

    <Style TargetType="{x:Type customControls:CheckButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type customControls:CheckButton}">
                    <Border x:Name="border" 
                            Background="Transparent" 
                            BorderBrush="Gray" 
                            BorderThickness="0" 
                            CornerRadius="{TemplateBinding CornerRadius}" 
                            Padding="5">
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
                            <Rectangle x:Name="checkMark" 
                                       Width="1" 
                                       Height="1" 
                                       Fill="Transparent" 
                                       Stroke="Gray" 
                                       StrokeThickness="2" 
                                       RadiusX="2" 
                                       RadiusY="2" 
                                       Visibility="Collapsed">
                            </Rectangle>
                            <TextBlock x:Name="text" 
                                       Text="{TemplateBinding Content}" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="WhiteSmoke"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="text" Property="FontWeight" Value="Bold"/>
                            <Setter TargetName="text" Property="Foreground" Value="Gray"/>
                            <!--<Setter TargetName="text" Property="Foreground" Value="{TemplateBinding CheckedBackground}"/>-->
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="DarkGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
